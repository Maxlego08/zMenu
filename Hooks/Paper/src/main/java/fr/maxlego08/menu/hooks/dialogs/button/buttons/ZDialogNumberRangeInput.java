package fr.maxlego08.menu.hooks.dialogs.button.buttons;

import fr.maxlego08.menu.api.button.buttons.dialogs.DialogNumberRangeInput;

public class ZDialogNumberRangeInput extends DialogNumberRangeInput {

    public ZDialogNumberRangeInput(
            String label,
            float start,
            float end,
            float step,
            String initialValue,
            int width,
            String labelFormat
    ) {
        super(label, start, end, step, initialValue, width, labelFormat);
    }
}
