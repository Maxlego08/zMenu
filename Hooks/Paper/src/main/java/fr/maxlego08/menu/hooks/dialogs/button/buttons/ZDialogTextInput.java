package fr.maxlego08.menu.hooks.dialogs.button.buttons;

import fr.maxlego08.menu.api.button.buttons.dialogs.DialogTextInput;

public class ZDialogTextInput extends DialogTextInput {

    public ZDialogTextInput(
            String label,
            Boolean labelVisible,
            String defaultValue,
            int width,
            int maxLength,
            int multilineMaxLines,
            int multilineHeight
    ) {
        super(label, labelVisible, defaultValue, width, maxLength, multilineMaxLines, multilineHeight);
    }
}
