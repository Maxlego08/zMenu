# Enhanced Bedrock Configuration Example

type: simple # Available: modal, simple, custom
name: "Simple Form"

buttons:
  button1:
    type: "bedrock_button"
    text: "Button 1"
    click-requirement:
      1:
        clicks: # Required to work, otherwise it will not work
          - ALL
        success:
          - type: message
            messages:
              - "&aAction for button 1"
  button2:
    type: "bedrock_button"
    text: "Button 2"
    image-type: "URL"
    image-value: "https://github.com/GeyserMC.png?size=200"
    click-requirement:
      1:
        clicks: # Required to work, otherwise it will not work
          - ALL
        success:
          - type: message
            messages:
              - "&aAction for button 2"
  button3:
    type: "bedrock_button"
    text: "Button 3"
    image-type: "PATH"
    image-value: "textures/i/glyph_world_template.png"
    click-requirement:
      1:
        clicks: # Required to work, otherwise it will not work
          - ALL
        success:
          - type: message
            messages:
              - "&aAction for button 3"